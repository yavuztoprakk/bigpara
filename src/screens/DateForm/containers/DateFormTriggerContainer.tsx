import React from "react";
import DateFormTrigger from "../components/DateFormTrigger";

const DateFormTriggerContainer = ({ navigation }) => {
  return <DateFormTrigger navigation={navigation} />;
};

export default DateFormTriggerContainer;
